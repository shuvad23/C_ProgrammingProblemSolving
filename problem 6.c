#include<stdio.h>
#include<conio.h>
int main()
{
    char ch;
    if(ch=printf(""))//if the printf("") inside value are not avilable the output is else; or the printf("") inside value are inclode the output is if;....
        printf("that is matter\n");
    else
        printf("that's dose not matter\n");
    return 0;
}
