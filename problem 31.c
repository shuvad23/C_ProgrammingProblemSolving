#include<stdio.h>
#include<conio.h>
int main()
{
    int a=0,b=5;
    if(a=0)//the (= )sine is indicate that the assignment operator not relational operator .so the if value of this condition is 0 .and the output result is else part;if i use the (==) this symbol  the result of this value is 1.and the output is if part;
        b=0;
    else
        b *=10;
        printf("b=%d",b);
    return 0;
}
